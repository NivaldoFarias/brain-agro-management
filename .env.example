# =============================================================================
# AGRO MANAGEMENT - ENVIRONMENT CONFIGURATION
# =============================================================================
#
# This file provides environment variable templates for all workspaces.
# Copy this to `.env` and configure the required variables.
#
# Environment schemas:
# - apps/api/src/config/env.ts (API variables)
# - apps/web/src/config/env.ts (Web variables)
# - packages/shared/src/utils/ (Shared utilities)
#
# LEGEND:
# - REQUIRED: Must be set in .env (no default value)
# - OPTIONAL: Has default value, can be overridden
#
# =============================================================================

# -----------------------------------------------------------------------------
# Shared Configuration (Used by all workspaces)
# -----------------------------------------------------------------------------

NODE_ENV=development              # Runtime environment: development|test|staging|production
LOG_LEVEL=info                    # Logging verbosity: trace|debug|info|warn|error|fatal
LOG_TO_CONSOLE=true               # Enable console logging in addition to file logs

# -----------------------------------------------------------------------------
# API Configuration (apps/api)
# Prefix: API_* for API-specific variables
# -----------------------------------------------------------------------------

API_PORT=3000                     # Port the API server will listen on
API_DATABASE_URL=                 # REQUIRED: PostgreSQL connection URL (postgresql://user:pass@host:port/db)
API_BASE_PATH=/api                # API base path for all endpoints

# -----------------------------------------------------------------------------
# Web Configuration (apps/web)
# Prefix: VITE_* for build-time variables (Vite convention)
# -----------------------------------------------------------------------------

VITE_API_BASE_URL=http://localhost:3000/api  # Backend API URL
VITE_ENABLE_DEVTOOLS=false                     # Enable React DevTools in production
